"use client";
// import { api } from "@/convex/_generated/api";
// import { useConvexQuery } from "@/hooks/use-convex-query";
import React from "react";
// import { BarLoader } from "react-spinners";

function CreatePost() {
  // const { data: existingDraft, isLoading: isDraftLoading } = useConvexQuery(api.posts.getDraftPost);
  // const { data: currentUser, isLoading: isUserLoading } = useConvexQuery(api.users.getCurrentUser);

  // if (isDraftLoading || isUserLoading) {
  //   return <BarLoader width={"100%"} color="#3b82f6" />;
  // }

  return <div>CreatePost</div>;
}

export default CreatePost;
